---
import HomePage from '../components/HomePage.astro';

// Server-rendered on purpose: Vercel serves prerendered files straight off the
// filesystem, ahead of the middleware, so a static `/` would never reach the
// locale detection in src/middleware.ts. Every other route stays static.
export const prerender = false;
---

<HomePage lang="es" />
